spring:
  application:
    name: simple-money-transfers
  datasource:
    url: "jdbc:h2:mem:money;MODE=PostgreSQL;DB_CLOSE_DELAY=-1"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    clean-disabled: true
